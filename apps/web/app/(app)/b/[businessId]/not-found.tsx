import { BusinessNotFound } from '@/components/shell/business-not-found'

// A page that does not exist inside a business: said inside the shell (D-091).
export default function NotFound() {
  return <BusinessNotFound />
}
