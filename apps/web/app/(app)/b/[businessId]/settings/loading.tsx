import { SettingsLoading } from '@/features/settings/settings-loading'

/** While a settings page is on its way: its title and placeholders, beside the section list. */
export default function Loading() {
  return <SettingsLoading />
}
