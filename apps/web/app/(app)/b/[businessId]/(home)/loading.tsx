import { Dashboard } from '@/features/dashboard/dashboard'

/**
 * While the Dashboard's page is on its way: the Dashboard itself, from what the shell and the layout
 * already have (the business, the checklist), so nothing moves when the page arrives.
 */
export default function DashboardLoading() {
  return <Dashboard />
}
