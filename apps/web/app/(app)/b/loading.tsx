import { ShellSkeleton } from '@/components/shell/shell-skeleton'

/** While a business opens (or another one is chosen): the shell's frame with placeholders. */
export default function BusinessLoading() {
  return <ShellSkeleton />
}
