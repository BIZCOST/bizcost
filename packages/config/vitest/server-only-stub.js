// Stands in for `server-only` under Vitest (the real package throws outside a React Server build).
// Vitest configs alias it: resolve.alias { 'server-only': '@bizcost/config/vitest/server-only-stub' }.
export {}
